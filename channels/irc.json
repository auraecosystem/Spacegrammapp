{
  channels: {
    irc: {
      enabled: true,
      dmPolicy: "pairing",
      configWrites: true,
      nickserv: {
        enabled: true,
        service: "server",
        password: "${IRC_NICKSERV_PASSWORD}",
        register: false,
        registerEmail: "webapp4@outlook.com",
      },
    },
  },
}
